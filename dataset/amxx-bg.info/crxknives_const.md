# crxknives_const.inc
```
#if defined _crxknives_const_included
    #endinput
#endif

#define _crxknives_const_included

/**
 * Default knife models
 */
#define CRXKNIVES_DEFAULT_V "models/v_knife.mdl"
#define CRXKNIVES_DEFAULT_P "models/p_knife.mdl"

/**
 * Maximum buffer length for knife sounds
 */
const CRXKNIVES_MAX_SOUND_LENGTH = 128

/**
 * Maximum buffer length for select messages
 */
const CRXKNIVES_MAX_MESSAGE_LENGTH = 128

/**
 * Maximum buffer length for attribute values
 */
const CRXKNIVES_MAX_ATTRIBUTE_LENGTH = 64
```


This code is a part of crxknives_const.inc. To use this code you should include crxknives_const.inc as ```#include <crxknives_const>```


  
  

Warning! This is an external include! It does not come bundled with AMX Mod X "out of the box" and requires additional installation. Use it only if you are absolutely certain that you need it.