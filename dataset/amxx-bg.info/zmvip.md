# Functions in zmvip.inc
Function | Description  
---|---  
[zv_register_extra_item](https://amxx-bg.info/api/zmvip/zv_register_extra_item) | ```
Registers extra items on this plugin for VIPs (Only if MODE 2 is active)
```
  
[zv_get_user_flags](https://amxx-bg.info/api/zmvip/zv_get_user_flags) | ```
Gets user flags (Only if MODE 1 is active)
```
  
[zv_extra_item_selected](https://amxx-bg.info/api/zmvip/zv_extra_item_selected) | ```
Called then player selects extra item in his VIP meniu. (Only if MODE 2 is active)

 You can stop player of buing item returning ZV_PLUGIN_HANDLED.
 Player will be refunded automaticaly
```
  

This code is a part of zmvip.inc. To use this code you should include zmvip.inc as ```#include <zmvip>```


  
  

Warning! This is an external include! It does not come bundled with AMX Mod X "out of the box" and requires additional installation. Use it only if you are absolutely certain that you need it.