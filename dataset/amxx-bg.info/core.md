# Functions in core.inc
Function | Description  
---|---  
[heapspace](https://amxx-bg.info/api/core/heapspace) | ```
Returns the free memory space available to the plugin.
```
  
[funcidx](https://amxx-bg.info/api/core/funcidx) | ```
Returns the function index of a public function declared in the plugin.
```
  
[numargs](https://amxx-bg.info/api/core/numargs) | ```
Returns the number of arguments passed into the currently executed function.
```
  
[getarg](https://amxx-bg.info/api/core/getarg) | ```
Retrieves an argument value passed into the currently executed function.
```
  
[setarg](https://amxx-bg.info/api/core/setarg) | ```
Sets the value of an argument passed into the currently executed function.
```
  
[tolower](https://amxx-bg.info/api/core/tolower) | ```
Converts a character to lowercase.
```
  
[toupper](https://amxx-bg.info/api/core/toupper) | ```
Converts a character to uppercase.
```
  
[swapchars](https://amxx-bg.info/api/core/swapchars) | ```
Swaps the bytes of a value (the lowest byte becomes the highest byte).
```
  
[random](https://amxx-bg.info/api/core/random) | ```
Returns a random number between 0 and a specified upper bound.
```
  
[min](https://amxx-bg.info/api/core/min) | ```
Returns the smaller of two provided values.
```
  
[max](https://amxx-bg.info/api/core/max) | ```
Returns the bigger of two provided values.
```
  
[clamp](https://amxx-bg.info/api/core/clamp) | ```
Limits a provided value between two specified bounds.
```
  
[power](https://amxx-bg.info/api/core/power) | ```
Returns a value raised to a specified exponent.
```
  
[sqroot](https://amxx-bg.info/api/core/sqroot) | ```
Returns the approximated square root of a value.
```
  
[time](https://amxx-bg.info/api/core/time) | ```
Retrieves the current time in hours, minutes and seconds.
```
  
[date](https://amxx-bg.info/api/core/date) | ```
Retrieves the current date in year, month and day.
```
  
[tickcount](https://amxx-bg.info/api/core/tickcount) | ```
Returns the elapsed CPU seconds.
```
  
[abs](https://amxx-bg.info/api/core/abs) | ```
Returns the absolute value of a number.
```
  

This code is a part of core.inc. To use this code you should include core.inc as ```#include <core>```


  
  

