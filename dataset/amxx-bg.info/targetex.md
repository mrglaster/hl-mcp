# Functions in targetex.inc
Function | Description  
---|---  
[cmd_targetex](https://amxx-bg.info/api/targetex/cmd_targetex) | ```
Processes a generic target pattern and tries to match it to a client or a
group of clients based on filtering flags and the usage of special arguments.
```
  
[targetex_is_team_argument](https://amxx-bg.info/api/targetex/targetex_is_team_argument) | ```
Checks whether a specified argument is a team argument.
```
  
[targetex_is_group_argument](https://amxx-bg.info/api/targetex/targetex_is_group_argument) | ```
Checks whether a specified argument is a group argument.
```
  
[targetex_is_no_exclude_argument](https://amxx-bg.info/api/targetex/targetex_is_no_exclude_argument) | ```
Checks whether a specified argument is a type of argument that doesn't
allow the admin to exclude himself.
```
  

This code is a part of targetex.inc. To use this code you should include targetex.inc as ```#include <targetex>```


  
  

Warning! This is an external include! It does not come bundled with AMX Mod X "out of the box" and requires additional installation. Use it only if you are absolutely certain that you need it.