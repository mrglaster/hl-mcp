# S2GConverter — Конвертер моделей из Source в GoldSource

## Описание S2GConverter

**S2GConverter** — это Python-утилита для конвертации 3D-моделей из движка **Source** (например, Half-Life 2, CS:GO) в формат, совместимый с устаревшим движком **GoldSource** (Half-Life 1, Counter-Strike 1.6).

Позволяет моддерам и разработчикам переносить современные модели в старые игры с сохранением анимаций и улучшенной визуальной детализации.

---

## Ключевые возможности S2GConverter

- **Поддержка любого количества полигонов** — нет ограничений на сложность геометрии.
- **Сохранение анимаций** — все анимационные последовательности переносятся корректно.
- **Генерация псевдо-нормалей (Pseudo-Normal Maps)** — автоматически создаётся текстура, имитирующая нормал-маппинг за счёт комбинирования диффузной текстуры и нормал-карты из `.vmt`.
- **Фьюжн текстур** — объединяет диффуз и нормаль для улучшения визуального качества в GoldSource.
- **Поддержка VTF/VMT** — автоматически обрабатывает текстуры и материалы Source Engine.

---

## Установка S2GConverter

0. Скачайте код с репозитория https://github.com/mrglaster/S2GConverter/
1. Убедитесь, что у вас установлен **Python 3**.
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Готово! Утилита готова к использованию.

---
https://github.com/mrglaster/S2GConverter/
## Структура файлов S2GConverter

Перед конвертацией поместите все файлы модели в одну директорию:

```
/models/my_model/
├── model.mdl    # Основной файл модели
├── model.vvd    # Вершинные данные
├── texture.vtf  # Текстура
└── texture.vmt  # Материал (должен ссылаться на bumpmap для генерации псевдо-нормалей)
```

---

## Использование S2GConverter

Запустите конвертацию командой:

```bash
python S2GConverter.py -i "path/to/your/model.mdl" [--generate_pnmaps | --no-generate_pnmaps]
```

### Аргументы:

| Аргумент                  | Тип      | По умолчанию | Описание |
|--------------------------|----------|--------------|----------|
| `-i`, `--input`          | `str` (обязательный) | — | Путь к `.mdl` файлу. Все связанные файлы должны находиться в той же папке. |
| `--generate_pnmaps`      | `boolean` | `True`       | Включает генерацию псевдо-нормальных карт на основе `bumpmap` из `.vmt`. |
| `--no-generate_pnmaps`   | `boolean` | —            | Отключает генерацию псевдо-нормалей. |

> Результат: `{source_model_name}_goldsource.mdl` в той же папке.

---

## Псевдо-нормальные карты (Pseudo-Normal Maps)

GoldSource не поддерживает настоящие нормальные карты. S2GConverter решает это, **комбинируя базовую текстуру с нормал-картой**, чтобы симулировать объёмное освещение и рельеф поверхности.

Это особенно полезно для моделей из современных игр (например, DOOM Eternal, Dark Souls), где важна детализация.

---

## Ограничения S2GConverter

Конвертация **невозможна**, если:

1. Анимационная последовательность превышает **64 КБ**.
2. Модель содержит **129 или более костей** (скелетных узлов).

---

## 💡 Применение S2GConverter

Идеально подходит для:

- Моддинга старых игр на GoldSource (Half-Life, CS 1.6, Ricochet и др.).
- Переноса современных активов в ретро-проекты.
- Обучения и экспериментов с 3D-конвертацией между движками.

---
