# `hlstocks.inc` — Half-Life AMX Mod X Stock Functions

##  Обзор

Этот файл предоставляет набор stock-функций для управления игроками, оружием, командами, счетом и другими игровыми параметрами в Half-Life через AMX Mod X. Все функции используют **gamedata**, а не хардкод, и совместимы с AMXX 1.9+.


## Константы

```c
#define HL_MAX_TEAMS           10
#define HL_TEAMNAME_LENGTH     16
#define HL_MAX_TEAMNAME_LENGTH 16
#define HL_MAX_WEAPON_SLOTS    6
#define HL_MAX_TEAMLIST_LENGTH 512
```

>  `HL_MAX_TEAMNAME_LENGTH` оставлен для обратной совместимости.

---

##  Функции

---

###  Очки и смерти

#### `hl_get_user_frags(id)`
> Возвращает количество фрагов игрока.

```c
stock hl_get_user_frags(id)
```

---

#### `hl_set_user_frags(id, frags)`
> Устанавливает количество фрагов игрока. Автоматически обновляет ScoreInfo.

```c
stock hl_set_user_frags(id, frags)
```

---

#### `hl_get_user_deaths(client)`
> Возвращает количество смертей игрока.

```c
stock hl_get_user_deaths(client)
```

---

#### `hl_set_user_deaths(client, deaths)`
> Устанавливает количество смертей игрока. Обновляет ScoreInfo.

```c
stock hl_set_user_deaths(client, deaths)
```

---

#### `hl_set_user_score(client, frags, deaths)`
> Устанавливает и фраги, и смерти одним сообщением (эффективнее, чем два отдельных вызова).

```c
stock hl_set_user_score(client, frags, deaths)
```

---

###  Оружие и боеприпасы

#### `hl_get_user_bpammo(client, weapon)`
> Возвращает количество патронов в рюкзаке для указанного оружия (HLW_*).

```c
stock hl_get_user_bpammo(client, weapon)
```

---

#### `hl_set_user_bpammo(client, weapon, ammo)`
> Устанавливает количество патронов в рюкзаке.

```c
stock hl_set_user_bpammo(client, weapon, ammo)
```

---

#### `hl_user_has_weapon(id, weapon_id)`
> Проверяет, есть ли у игрока указанное оружие. Возвращает entity index или 0.

```c
stock hl_user_has_weapon(id, weapon_id)
```

---

#### `hl_strip_user_weapon(id, weapon_id)`
> Удаляет указанное оружие у игрока.

```c
stock hl_strip_user_weapon(id, weapon_id)
```

---

#### `hl_strip_user_weapons(client)`
> Удаляет все оружие у игрока.

```c
stock hl_strip_user_weapons(client)
```

---

#### `hl_get_user_weapon_ent(client)`
> Возвращает entity активного оружия.

```c
stock hl_get_user_weapon_ent(client)
```

---

#### `hl_get_weapon_ammo(entity)`
> Возвращает количество патронов в магазине оружия.

```c
stock hl_get_weapon_ammo(entity)
```

---

#### `hl_set_weapon_ammo(entity, clip)`
> Устанавливает количество патронов в магазине.

```c
stock hl_set_weapon_ammo(entity, clip)
```

---

#### `hl_get_weapon_id(entity)`
> Возвращает ID оружия (соответствует HLW_*).

```c
stock hl_get_weapon_id(entity)
```

---

###  Ящики с оружием (WeaponBox)

#### `hl_get_wbox_weapon_ent(entity)`
> Возвращает entity оружия внутри ящика.

```c
stock hl_get_wbox_weapon_ent(entity)
```

---

#### `hl_get_wbox_ammo(entity)`
> Возвращает количество патронов в ящике.

```c
stock hl_get_wbox_ammo(entity)
```

---

#### `hl_set_wbox_ammo(entity, ammo)`
> Устанавливает количество патронов в ящике.

```c
stock hl_set_wbox_ammo(entity, ammo)
```

---

#### `hl_remove_wbox(entity)`
> Корректно удаляет ящик с оружием и все его содержимое.

```c
stock hl_remove_wbox(entity)
```

---

###  Команды и модель

#### `hl_get_user_team(client, team[] = "", len = 0)`
> Возвращает ID команды игрока. Если указан буфер — копирует название команды.

```c
stock hl_get_user_team(client, team[] = "", len = 0)
```

---

#### `hl_set_user_team(client, const team[])`
> Устанавливает команду игрока по названию. Не убивает игрока и не выкидывает из спектатора.

```c
stock hl_set_user_team(client, const team[])
```

---

#### `hl_set_user_teamnames(id, any:...)`
> Устанавливает названия команд в табло. Поддерживает до 10 команд.

```c
stock hl_set_user_teamnames(id, any:...)
// Пример: hl_set_user_teamnames(id, "Blue", "Red");
```

---

#### `hl_set_user_teamscore(id, teamid, frags, deaths = 0)`
> Устанавливает общий счет команды в табло. Использует `teamid`, а не название.

```c
stock hl_set_user_teamscore(id, teamid, frags, deaths = 0)
```

---

###  Здоровье и броня

#### `hl_get_user_health(client)`
> Возвращает текущее здоровье игрока.

```c
stock hl_get_user_health(client)
```

---

#### `hl_set_user_health(client, health)`
> Устанавливает здоровье. Если ≤0 — убивает игрока.

```c
stock hl_set_user_health(client, health)
```

---

#### `hl_get_user_armor(client)`
> Возвращает текущий уровень брони.

```c
stock hl_get_user_armor(client)
```

---

#### `hl_set_user_armor(client, armorvalue)`
> Устанавливает уровень брони.

```c
stock hl_set_user_armor(client, armorvalue)
```

---

### 👁️‍🗨️ Спектатор и LongJump

#### `hl_get_user_spectator(client)`
> Проверяет, находится ли игрок в режиме спектатора.

```c
stock bool:hl_get_user_spectator(client)
```

---

#### `hl_set_user_spectator(client, bool:spectator = true)`
> Переключает игрока в/из режима спектатора. Отправляет Spectator-сообщение.

```c
stock hl_set_user_spectator(client, bool:spectator = true)
```

---

#### `hl_get_user_longjump(client)`
> Проверяет, активен ли longjump у игрока.

```c
stock bool:hl_get_user_longjump(client)
```

---

#### `hl_set_user_longjump(client, bool:longjump = true, bool:tempicon = true)`
> Включает/выключает longjump. Может показать иконку подбора.

```c
stock hl_set_user_longjump(client, bool:longjump = true, bool:tempicon = true)
```

---

### 🧬 Вспомогательные функции (внутренние)

> Эти функции используются внутри библиотеки и не предназначены для прямого вызова.

- `__get_user_team(client, team[] = "", len = 0)`
- `__get_team_index(const team[])`
- `__count_teams()`
- `__explode_teamlist(output[][], size, input[], delimiter)`
- `__get_team_index__(const teamname[], team_names[][], numTeams)`

---

##  История версий

| Версия | Дата       | Автор | Изменения |
|--------|------------|-------|-----------|
| 1.2    | 02.10.2013 | Kord  | Первая версия |
| 1.3    | 22.12.2018 | rtxa  | Gamedata, Spectator msg, HL_MAX_TEAMNAME_LENGTH, frags/deaths |
| 1.4    | 14.03.2019 | rtxa  | Переименование, исправления, MSG_ALL, не убивает при смене команды |
| 1.5    | 15.03.2019 | rtxa  | Добавлен `hl_set_user_score()` |
| 1.6    | 03.03.2020 | rtxa  | Возврат `HL_MAX_TEAMNAME_LENGTH`, исправления |
| 1.7    | 10.04.2020 | —     | Исправлена опечатка в `hl_set_user_score()` |
| 1.8    | 06.05.2020 | —     | Добавлены `hl_user_has_weapon()`, `HL_MAX_WEAPON_SLOTS` |
| 1.9    | 12.05.2020 | —     | Добавлен `hl_strip_user_weapon()` |
| 2.0    | 23.06.2020 | —     | Исправлен баг со счетом в спектаторе |
| 2.1    | 26.06.2020 | —     | Добавлены `hl_set_user_teamnames()`, `hl_set_user_teamscore()` |
| 2.2    | 04.07.2020 | —     | Добавлен `hl_remove_wbox()`, чистка кода |
| 2.3    | 06.07.2020 | —     | Улучшена обработка списка команд, добавлен `HL_MAX_TEAMLIST_LENGTH` |
| 2.4    | 08.07.2020 | —     | Исправлены опечатки в `hl_remove_wbox()` |
| 2.5    | 18.07.2020 | —     | Использование `teamid` вместо `teamname` в `hl_set_user_teamscore()` |

---

## 📎 Примечания

- Все функции используют `gamedata`, что делает их совместимыми с разными модами Half-Life.
- Для корректной работы требуется AMX Mod X 1.9+.
- Сообщения (`ScoreInfo`, `TeamInfo`, `Spectator`) отправляются через `MSG_ALL` для корректного отображения на всех клиентах.
- Функции, начинающиеся с `__`, являются внутренними и не должны вызываться напрямую.

---

##  См. также

- `hlsdk_const.inc` — для значений `HLW_*`
- `fakemeta`, `hamsandwich` — зависимости
- `amxmodx` — основной модуль
